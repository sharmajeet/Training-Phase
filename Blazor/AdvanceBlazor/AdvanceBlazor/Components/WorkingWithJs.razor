 @page "/blazorjs";
 @*
@inject IJSRuntime _JSRuntime;
<div class="container">
    <button class="btn-primary" @onclick="testConfirmBtn">Test Confirm Button</button>
</div>

@code {
    private string ConfirmMessage = "Are you sure you want to delete this item?";

    private async Task testConfirmBtn()
    {
        Console.WriteLine("Test Confirm Button Clicked");
        // Call the JavaScript confirm dialog and capture the result
        await _JSRuntime.InvokeAsync<bool>("confirm", ConfirmMessage);


    }
} *@



<PageTitle>Counter</PageTitle>

<h1>Counter</h1>

<p role="status">Current count: @currentCount</p>

<button class="btn btn-primary" @onclick="IncrementCount">Click me</button>

@code {
    private int currentCount = 0;

    private void IncrementCount()
    {
        currentCount++;
    }
}
